/**
 * SRS U5 — shared kitchen display credential (wall / KDS).
 * Sent on PATCH status updates and Socket.io `join_kitchen` when configured.
 */
export const KITCHEN_DISPLAY_TOKEN_HEADER = "x-roms-kitchen-token";
