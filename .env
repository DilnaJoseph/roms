# Local dev only (gitignored). Change in production.
JWT_SECRET=dev-only-roms-jwt-secret-min-32-chars!!

# MySQL 8.x — required for Prisma to persist orders (same DB for POST + U6 PATCH).
# Uncomment and set user, password, host, database name (must match `npx prisma migrate dev`).
DATABASE_URL="mysql://root:mysql_Dil05@127.0.0.1:3306/roms?allowPublicKeyRetrieval=true"
